--- this is a test line ---

Avant de compiler, installer cminpack a l'adresse http://devernay.free.fr/hacks/cminpack.html.
make & sudo make install:)


packages:

subversion

-- pour png --
libpng12-dev (obligatoire)

-- pour ppm --
libnetpbm10-dev

-- pour sift++ --
g++ (a tester dans la config!)


-- pour ffmpeg --

libavcodec-dev libavformat-dev libavutil-dev libswscale-dev



cmake . && make && sudo make install
RIEN!!!!



